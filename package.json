{
  "name": "ts-tools",
  "version": "0.0.1",
  "license": "MIT",
  "repository": "https://bitbucket.org/martinmo/ts-tools",
  "scripts": {
    "cleanup": "lerna exec -- del-cli dist node_modules coverage",
    "bootstrap": "lerna bootstrap",
    "validate": "lerna run validate",
    "build": "lerna run build",
    "full-build": "yarn bootstrap && yarn build && yarn validate && yarn build"
  },
  "devDependencies": {
    "del-cli": "^1.1.0",
    "lerna": "^2.9.0"
  }
}
