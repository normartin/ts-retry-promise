{
  "name": "ts-tools",
  "version": "0.0.1",
  "license": "MIT",
  "repository": "https://bitbucket.org/martinmo/ts-tools",
  "scripts": {
    "cleanup": "lerna exec -- del-cli dist node_modules coverage",
    "bootstrap": "lerna bootstrap",
    "test": "lerna run test",
    "build": "lerna run build",
    "test:coverage": "lerna run test:coverage",
    "full-build": "yarn cleanup && yarn bootstrap && yarn test:coverage && yarn build"
  },
  "devDependencies": {
    "del-cli": "^1.1.0",
    "lerna": "^2.9.0"
  }
}
